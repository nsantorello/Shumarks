        <% if logged_in? %>
            <%= image_tag(current_user.profile_image_url, :width => 24, :height => 24) %> Logged in as <strong>@<%= current_user.login %></strong>. <%= link_to 'Log out', 'http://localhost:3001/logout' %>
          <% else %>
            <%= link_to 'Login via Twitter', 'http://localhost:3001/login', :id => "twit_status" %>
          <% end %>
